/*Function (функции)

const showMessage = () => {           - стрелочная функция
    console.log("Это сообщение из тела функции showMessage");
}; 
'show' частица определяющая какое-то сообщение, отвечает на вопрос 'что делает?'

showMessage();
showMessage();
showMessage();

function showMessage() {
    console.log("Это сообщение из тела функции showMessage");
}

showMessage();

let boyName = 'Дмитрий'
let boyAge = 20

let boyName = 'Андрей'
let boyAge = 17

let boyName = 'Сергей'
let boyAge = 24

const checkAge = (name, age) => {
    if (age > 18) {
        console.log(name + " идет в армию");
    } else {
        console.log(name + " остается дома");
    }
};

checkAge("Дмитрий", 20);
checkAge("Андрей", 17);
checkAge("Сергей", 24);


const sum = (x, y, z) => {
    return x + y + z;
};

const result = sum(30, 45, 5);

console.log(result);

const result1 = result + 10;
console.log(result1);

*/
